# Core Dependencies (MUST HAVE)
requests>=2.28.0
dnspython>=2.3.0
python-whois>=0.8.0
python-nmap>=0.7.1
beautifulsoup4>=4.11.0
jinja2>=3.1.0
colorama>=0.4.6
tqdm>=4.65.0
markdown>=3.4.3

# Performance & Optimization (TO FIX SLOW SCANS)
concurrent-log-handler>=0.9.24
python-json-logger>=2.0.7
psutil>=5.9.0
PySocks>=1.7.1  # For proxy support if needed

# Error Handling & Stability (TO PREVENT CRASHES)
retrying>=1.3.4
backoff>=2.2.1
timeout-decorator>=0.5.0
tenacity>=8.2.2

# Network & Security (FOR BETTER SCANNING)
pyOpenSSL>=23.2.0
cryptography>=41.0.0
urllib3>=1.26.0  # Updated for security

# CLI Enhancement (FOR BETTER USER EXPERIENCE)
click>=8.1.0
rich>=13.5.0  # For beautiful console output
pyfiglet>=0.8.post1  # For banners
tabulate>=0.9.0  # For table formatting

# Development & Debugging (FOR TROUBLESHOOTING)
ipdb>=0.13.13
python-dotenv>=1.0.0
black>=23.3.0
flake8>=6.0.0

# Optional (FOR ADVANCED FEATURES - COMMENT OUT IF NOT NEEDED)
# aiodns>=3.0.0  # Async DNS for faster lookups
# alive-progress>=3.1.0  # Better progress bars
# censys>=2.2.0  # Censys API (requires API key)
# shodan>=1.29.0  # Shodan API (requires API key)
# virustotal-api>=1.1.11  # VirusTotal API (requires API key)
# censys>=2.2.0  # Censys API

# Platform Specific (LINUX/MAC)
# No special packages needed

# Windows Specific (IF PORTING TO WINDOWS LATER)
# pywin32>=306  # Windows API access
# wmi>=1.5.1  # Windows Management Instrumentation
